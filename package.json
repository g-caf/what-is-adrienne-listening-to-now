{
  "name": "spotify-now-playing",
  "version": "1.0.0",
  "description": "Standalone now-playing Spotify widget service.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.22.2"
  }
}
